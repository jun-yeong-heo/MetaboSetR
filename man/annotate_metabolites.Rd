% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotate_metabolites}
\alias{annotate_metabolites}
\title{Annotate Biocrates metabolites with cross-reference database IDs}
\usage{
annotate_metabolites(x)
}
\arguments{
\item{x}{Character vector of metabolite \code{short_name}s, e.g. the
\code{colnames} of a metabolite assay.}
}
\value{
A data.frame with one row per element of \code{x} and columns
\code{metabolite} (the input name), \code{long_name}, \code{class}, and the id columns
\code{kegg_id}, \code{hmdb_id}, \code{pubchem_id}, \code{chebi_id}, \code{refmet_id},
\code{lipidmaps_id}, \code{lion_id}. Cells are \code{NA} where unknown.
}
\description{
Maps Biocrates \code{short_name}s to the external-database identifiers held in
the built-in metabolite dictionary (KEGG, HMDB, PubChem, ChEBI, RefMet,
LIPID MAPS, LION). One representative id per metabolite per source; see
DESIGN.md §9. The result always has one row per input element, in input
order, with \code{NA} for unmatched names or missing ids — ready to feed into
KEGG-map overlays (\code{pathview}) or external pathway tools.
}
\details{
This exposes the id annotations the package already curates so callers no
longer need to reach into the internal dictionary directly.
}
\examples{
annotate_metabolites(c("Nicotine", "TMAO", "not_a_metabolite"))
}
