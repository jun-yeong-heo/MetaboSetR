% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-feature.R
\name{test_metabolites}
\alias{test_metabolites}
\title{Feature-level group testing of metabolites}
\usage{
test_metabolites(
  prep,
  group,
  method = c("limma", "wilcoxon"),
  padjust = c("BH", "BY")
)
}
\arguments{
\item{prep}{A \link{PreprocessedData} object.}

\item{group}{Either the name of a column in \code{prep@sample@sample_meta} or
a vector of length \code{nrow(prep@sample@assay)} (all samples, before
outlier exclusion).}

\item{method}{One of \code{"limma"} (default) or \code{"wilcoxon"}.}

\item{padjust}{Character vector of multiple-testing methods to report;
each becomes a \verb{padj_<method>} column. Default \code{c("BH", "BY")}.}
}
\value{
A data.frame with one row per metabolite.
}
\description{
Approach A from DESIGN.md §6.1: treat each metabolite as an individual
feature and run a group comparison via limma moderated t (default) or
Wilcoxon rank-sum, with multiple-testing correction. Concentrations are
log2-transformed with an adaptive per-feature pseudocount before testing
(DESIGN.md §4.5). Outlier samples (\verb{@sample_outliers}) are excluded.
}
\details{
Metabolite-to-metabolite correlation is high within a chemical class, so
the BH/BY pair is reported by default (decisions.md #29).
}
