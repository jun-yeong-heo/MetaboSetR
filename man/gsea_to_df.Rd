% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-gsea.R
\name{gsea_to_df}
\alias{gsea_to_df}
\title{Convert an fgsea result to a flat data.frame for export}
\usage{
gsea_to_df(res, sep = ";")
}
\arguments{
\item{res}{An fgsea-style result, typically from \code{\link[=gsea_pathway_sets]{gsea_pathway_sets()}}.}

\item{sep}{Single-character separator used to join list-column
members. Default \code{";"}.}
}
\value{
A \code{data.frame} with the same rows and columns as \code{res}; any
list column (e.g. \code{leadingEdge}) becomes character.
}
\description{
\code{\link[=gsea_pathway_sets]{gsea_pathway_sets()}} returns the raw fgsea \code{data.table}, whose
\code{leadingEdge} column is a list of character vectors. R's
\code{write.table()} / \code{write.csv()} cannot encode list columns and fail with
\verb{unimplemented type 'list' in 'EncodeElement'}. This helper collapses
every list column with \code{paste(..., collapse = sep)} and returns a plain
data.frame so the result can be written to TSV/CSV directly. Non-list
columns are passed through unchanged. See DESIGN.md §6.2.3 and
decisions.md #53.
}
\examples{
\dontrun{
  res <- gsea_pathway_sets(prep, group = "Response", domain = "reactome")
  write.table(gsea_to_df(res), "reactome_gsea.tsv",
              sep = "\t", row.names = FALSE, quote = FALSE)
}
}
