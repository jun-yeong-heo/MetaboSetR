% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{preprocess}
\alias{preprocess}
\title{Run the preprocessing pipeline}
\usage{
preprocess(
  conc,
  qc,
  thresholds = NULL,
  sample_outlier_method = c("IQR", "percentile", "both"),
  outlier_metric = c("below_lod", "above_uloq", "either"),
  percentile_cutoff = 0.95,
  cv_threshold = 20,
  cv_mode = c("per_kit", "pooled"),
  lod_rate_threshold = 0.5,
  apply_lod_rate_filter = TRUE,
  winsorize_uloq = FALSE
)
}
\arguments{
\item{conc}{A \link{ConcentrationData} for sample data.}

\item{qc}{A \link{QCData} for pooled QC.}

\item{thresholds}{Optional \link{ThresholdData}. Required if
\code{winsorize_uloq = TRUE}.}

\item{sample_outlier_method}{One of \code{"IQR"} (default), \code{"percentile"},
or \code{"both"}. See DESIGN.md §4.2.}

\item{outlier_metric}{One of \code{"below_lod"} (default), \code{"above_uloq"},
or \code{"either"}. Picks which per-sample rate drives outlier flagging.}

\item{percentile_cutoff}{Quantile for the \code{"percentile"} method
(default 0.95).}

\item{cv_threshold}{CV cutoff in percent (default 20).}

\item{cv_mode}{\code{"per_kit"} (AND across kits, default) or \code{"pooled"}.}

\item{lod_rate_threshold}{Maximum allowed \verb{<LOD} rate per metabolite
(default 0.5).}

\item{apply_lod_rate_filter}{If \code{FALSE}, skip the LOD-rate step
(Biocrates-strict CV-only protocol).}

\item{winsorize_uloq}{Replace \verb{> ULOQ} cells with the kit ULOQ. Requires
\code{thresholds} and silently skips cells whose ULOQ is \code{NA}.}
}
\value{
A \link{PreprocessedData} object.
}
\description{
Combine sample and QC data into a \link{PreprocessedData} object after running
sample-level outlier detection and the two-step metabolite filter
(CV first, then LOD rate). Filter results are recorded as flags;
the raw \code{assay} matrices are not removed (DESIGN.md §4 / §5.4).
}
