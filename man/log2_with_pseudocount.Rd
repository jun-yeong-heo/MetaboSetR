% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{log2_with_pseudocount}
\alias{log2_with_pseudocount}
\title{log2 transform with an adaptive per-feature pseudocount}
\usage{
log2_with_pseudocount(x, pseudocount = NULL)
}
\arguments{
\item{x}{A numeric matrix (rows = features) or a numeric vector
(one feature).}

\item{pseudocount}{Optional scalar. If given, used directly for every
cell; otherwise the per-feature half-min rule applies.}
}
\value{
A matrix or vector of the same shape as \code{x}, log2-transformed.
}
\description{
Applies \code{log2(x + c)} where the pseudocount \code{c} is chosen \emph{per row}
(i.e. per feature) as half the smallest finite positive value in that
row whenever the row contains a zero, and is \code{0} (no offset) otherwise.
This is the same offset rule the package's GSEA / QC / DE layers use
internally before any statistical test (DESIGN.md §4.5, decisions.md
#25-26).
}
\details{
Conventions:
\itemize{
\item \code{x} is treated row-wise: rows are features (metabolites), columns
are samples. Pass \code{t(...)} if your input has features as columns.
\item A vector input is treated as one feature.
\item Inf / NaN cells pass through (log2 of Inf is Inf, of NaN is NaN).
\item If \code{pseudocount} is supplied explicitly, the same scalar is added
to every cell (legacy "global" behaviour, retained for callers
that want to override the per-feature default).
}
}
\examples{
x <- rbind(low  = c(0, 0.1, 0.5, 1),
           high = c(100, 200, 0, 400))
log2_with_pseudocount(x)
}
