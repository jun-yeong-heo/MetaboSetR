% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-ora.R
\name{ora_pathway_sets}
\alias{ora_pathway_sets}
\title{Over-representation analysis over curated pathway sets (Approach C)}
\usage{
ora_pathway_sets(sig, universe, domain, padjust = c("BH", "BY"))
}
\arguments{
\item{sig}{Character vector of significant metabolite names.}

\item{universe}{Character vector of all measured/tested metabolite names
(the background panel). Must be non-empty; \code{sig} is intersected with it.}

\item{domain}{One of the available pathway-set domains; see
\code{\link[=list_pathway_sets]{list_pathway_sets()}}.}

\item{padjust}{Character vector of multiple-testing methods to report;
each becomes a \verb{padj_<method>} column. Default \code{c("BH", "BY")}.}
}
\value{
A data.frame with one row per pathway that has at least one
member in \code{universe}, ordered by ascending \code{pval}. Columns:
\code{set_name}, \code{set_size} (members present in \code{universe}), \code{overlap}
(significant members in the set), \code{expected} (overlap expected by
chance), \code{pval}, one \verb{padj_<method>} per \code{padjust}, and
\code{overlap_members} (the overlapping members, \verb{;}-joined).
}
\description{
Tests whether a threshold-selected set of significant metabolites is
over-represented in each curated pathway set, using a one-sided
hypergeometric test. This is the threshold-based complement to the
rank-based \code{\link[=gsea_pathway_sets]{gsea_pathway_sets()}}; both run over the same built-in
\code{pathway_sets}. See DESIGN.md §6.6.
}
\details{
The background (\code{universe}) is the \emph{measured panel}: the full set of
metabolites you tested. Restricting the background to what was actually
measured is the honest choice for a fixed Biocrates panel — a metabolite
you never measured can never be significant, so it does not belong in the
denominator (decisions.md #68). Each pathway is intersected with
\code{universe} before testing; members outside \code{universe} are dropped.

\code{sig} and \code{universe} are metabolite \code{short_name}s — the same identifier
space as the pathway-set members (see \code{\link[=get_pathway_sets]{get_pathway_sets()}}).

Direction information in \code{pathway_sets_meta} is \emph{not} used by the test;
it is interpretation metadata only, mirroring GSEA (decisions.md #40).
}
\examples{
\dontrun{
  res <- test_metabolites(prep, group = "treatment")
  ora_pathway_sets(
    sig      = res$metabolite[res$P.Value < 0.05],
    universe = res$metabolite,
    domain   = "reactome")
}
}
